APP_NAME=FlowPilot
POSTGRES_PASSWORD=replace_with_a_local_password
# Keep this password consistent with POSTGRES_PASSWORD (URL-encode if needed).
DATABASE_URL=postgresql+psycopg://flowpilot:replace_with_a_local_password@localhost:5432/flowpilot
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1
